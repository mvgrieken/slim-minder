[build]
  command = "npm ci && npm run build --prefix packages/utils && cd apps/mobile && npm run build:web"
  publish = "apps/mobile/dist"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
